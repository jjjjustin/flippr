<%= link_to photo_for(current_user), current_user %>
<h1><%= current_user.user_name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.flips.count, "flips") %></span>
